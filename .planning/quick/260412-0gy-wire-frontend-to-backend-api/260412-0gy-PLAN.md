---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend/lib/api.ts
  - Frontend/app/(app)/dashboard/page.tsx
  - Frontend/app/(app)/timeline/[dependent_id]/page.tsx
  - Frontend/package.json
autonomous: true
requirements: []
---

<objective>
Wire Frontend to Backend API, remove hardcoded data, add Prisma client
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Backend API routes:
- /api/v1/households (GET, POST, GET/:id, PATCH/:id, DELETE/:id)
- /api/v1/dependents (GET, POST, GET/:id, PATCH/:id, DELETE/:id)
- /api/v1/timeline (GET /:dependent_id)

Prisma schema available at Frontend/prisma/schema.prisma matching Backend SQLModel
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Frontend API client library</name>
  <files>Frontend/lib/api.ts</files>
  <action>Create Frontend/lib/api.ts with typed fetch functions for calling Backend API:
- getHouseholds(): Promise&lt;Household[]&gt;
- getHousehold(id: string): Promise&lt;Household&gt;
- getDependents(householdId?: string): Promise&lt;Dependent[]&gt;
- getTimeline(dependentId: string): Promise&lt;HealthEvent[]&gt;

Use environment variable NEXT_PUBLIC_API_URL (defaults to http://localhost:8000). Use standard fetch, not axios. Handle errors with proper HTTP status checking and throw descriptive errors.</action>
  <verify>File created with all 4 exported functions</verify>
  <done>API client library created with typed functions for households, dependents, and timeline</done>
</task>

<task type="auto">
  <name>Task 2: Connect Dashboard to backend API</name>
  <files>Frontend/app/(app)/dashboard/page.tsx</files>
  <action>Replace hardcoded "Sharma Family" with data fetched from /api/households. Use React useState + useEffect to fetch on mount. Show loading state while fetching. Handle errors gracefully (show "Unable to load data" message).</action>
  <verify>App runs and displays household name from API</verify>
  <done>Dashboard shows real household name from backend, not hardcoded</done>
</task>

<task type="auto">
  <name>Task 3: Connect Timeline page to backend API</name>
  <files>Frontend/app/(app)/timeline/[dependent_id]/page.tsx</files>
  <action>Replace hardcoded events array with data fetched from /api/v1/timeline/:dependent_id. Use the dynamic route param dependent_id in the API call. Parse API response to match component shape. Handle loading and error states. ALSO add @prisma/client to Frontend dependencies and run npx prisma generate.</action>
  <verify>Timeline page shows events from API for each dependent</verify>
  <done>Timeline page displays real health events, not hardcoded</done>
</task>

</tasks>

<verification>
Frontend makes real API calls to Backend and displays dynamic data
</verification>

<success_criteria>
- Frontend/lib/api.ts exists with typed functions
- Dashboard shows household from database
- Timeline shows events from database
- Prisma client generates successfully
</success_criteria>

<output>
After completion, create `.planning/quick/260412-0gy-wire-frontend-to-backend-api/260412-0gy-SUMMARY.md`
</output>