---
mode: quick
id: 260412-4tk
type: execute
autonomous: true
scope: atomic
files_modified:
  - Frontend/components/VoiceFAB.tsx
  - Frontend/components/AppLayout.tsx
---

<objective>
Fix Vapi `start-method-error` Bad Request in VoiceFAB and make the init/start/stop flow robust so failures are handled gracefully without leaving the UI in a broken state.
</objective>

<assumptions>
- Frontend-only change; backend webhook/tool routes stay unchanged.
- `@vapi-ai/web` remains at current project version (`^2.5.2`).
- Live calls require valid `NEXT_PUBLIC_VAPI_PUBLIC_KEY` and `NEXT_PUBLIC_VAPI_ASSISTANT_ID`; missing prerequisites should degrade gracefully.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Harden VoiceFAB initialization and Vapi start payload</name>
  <files>Frontend/components/VoiceFAB.tsx</files>
  <action>Refactor VoiceFAB to use guarded start helpers: initialize SDK once, enforce preflight checks before `vapi.start` (SDK ready, keys present, required local context), and normalize outbound assistant overrides so the start request is valid for Vapi Web SDK. Add explicit error parsing for `start-method-error`/Bad Request and map failures to a clean fallback state (not stuck in connecting, not falsely active).</action>
  <verify>
    1. `cd Frontend && npm run lint -- components/VoiceFAB.tsx`
    2. `cd Frontend && npm run build`
    3. Manual focused check on `/dashboard`: attempt one normal start and one failure path (e.g., missing local context). In both cases, confirm no uncaught exception and the button returns to a valid idle/error-recoverable state.
  </verify>
  <done>VoiceFAB no longer surfaces unhandled Vapi Bad Request start failures; all start failures are captured, logged with actionable context, and recovered in UI state.</done>
</task>

<task type="auto">
  <name>Task 2: Prevent duplicate starts and stabilize lifecycle ownership</name>
  <files>Frontend/components/VoiceFAB.tsx, Frontend/components/AppLayout.tsx</files>
  <action>Add a single in-flight start guard and deterministic stop/reset flow in VoiceFAB (including listener/timer cleanup on unmount). In AppLayout, remove duplicate simultaneous VoiceFAB mounts and keep one shared instance with responsive positioning to avoid multiple SDK instances racing start/stop logic.</action>
  <verify>
    1. `cd Frontend && npm run lint -- components/VoiceFAB.tsx components/AppLayout.tsx`
    2. `cd Frontend && npm run build`
    3. Manual focused check: rapidly tap mic 5-10 times and resize between mobile/desktop breakpoints; confirm only one call flow is active and UI never remains stuck in connecting.
  </verify>
  <done>Only one VoiceFAB instance controls Vapi lifecycle, concurrent start attempts are blocked, and stop/failure always converges to a clean idle state.</done>
</task>

</tasks>

<definition_of_done>
- Vapi `start-method-error` Bad Request is handled gracefully in frontend call flow.
- Voice start/stop lifecycle is resilient to retries, rapid toggles, and missing prerequisites.
- Frontend lint and production build pass after the targeted edits.
</definition_of_done>

<out_of_scope>
- Backend Vapi webhook/tool contract changes
- Nonessential UI redesign outside minimal failure-state handling
</out_of_scope>