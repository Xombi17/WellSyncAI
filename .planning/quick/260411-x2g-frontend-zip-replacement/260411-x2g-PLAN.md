---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend/
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Frontend replaced with zip content and runs without errors
    - API calls connect to Backend at localhost:8000
    - Key pages (households, dependents, timeline, medicine) are accessible
  artifacts:
    - path: Frontend/src/app
      provides: Next.js App Router pages
    - path: Frontend/package.json
      provides: Dependencies for frontend
    - path: Frontend/.env
      provides: Environment configuration with API URL
  key_links:
    - from: Frontend/
      to: http://localhost:8000
      via: Environment variable NEXT_PUBLIC_API_URL
---

<objective>
Replace current Frontend with extracted zip content from wellsync-ai (2).zip, fix dependency/version conflicts, and connect to the running Backend API.

Purpose: Bring in the existing frontend implementation and integrate it with the backend.
Output: Working frontend connected to backend API.
</objective>

<context>
@PRD.md
@AGENTS.md

Current Frontend state:
- Next.js 16.2.3 with App Router
- Uses Prisma client, TanStack Query, react-hook-form, zod
- Has pages: households, dependents, timeline, dashboard, medicine, reminders, settings

Zip file content:
- Next.js 15.x (needs upgrade to 16.2.3 per PRD)
- Missing Prisma, TanStack Query, shadcn UI
- Has working pages: households, dependents, timeline, dashboard, medicine, reminders
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract zip and backup current Frontend</name>
  <files>Frontend/</files>
  <action>
1. Create backup of current Frontend: `cp -r Frontend Frontend.backup`
2. Extract zip to temp location: `cd /tmp && unzip -o "/Users/varad/Downloads/wellsync-ai (2).zip" -d frontend-zip`
3. Copy extracted files to Frontend, overwriting existing files
4. Remove conflicting files from zip that shouldn't overwrite (like .env if different)
</action>
  <verify>
    <automated>ls -la Frontend/app/ | head -10</automated>
  </verify>
  <done>Zip extracted, Frontend directory has new structure</done>
</task>

<task type="auto">
  <name>Task 2: Fix package.json and install dependencies</name>
  <files>Frontend/package.json</files>
  <action>
1. Read the new Frontend/package.json
2. Merge with original PRD-requirement packages:
   - @prisma/client ^7.7.0
   - @tanstack/react-query ^5.97.0
   - next 16.2.3
   - shadcn UI components (class-variance-authority, clsx, tailwind-merge)
3. Update package.json with merged dependencies
4. Run `cd Frontend && bun install` or `npm install`
</action>
  <verify>
  <automated>cd Frontend && cat package.json | grep -E '"next"|"@tanstack"|"@prisma"' | head -5</automated>
  </verify>
  <done>Dependencies installed, Next.js 16.2.3 in use</done>
</task>

<task type="auto">
  <name>Task 3: Configure API connection to Backend</name>
  <files>Frontend/.env</files>
  <action>
1. Ensure Frontend/.env has: `NEXT_PUBLIC_API_URL=http://localhost:8000`
2. Check existing API client code (likely in lib/ or hooks/) and update base URL
3. Verify API routes exist in extracted code or create api client wrapper
4. Ensure CORS is handled on Backend (Backend should allow localhost:3000)
</action>
  <verify>
  <automated>grep "NEXT_PUBLIC_API_URL" Frontend/.env && curl -s http://localhost:8000/api/v1/health 2>/dev/null | head -1</automated>
  </verify>
  <done>Frontend can make requests to Backend API</done>
</task>

<task type="auto">
  <name>Task 4: Build and verify frontend runs</name>
  <files>Frontend/</files>
  <action>
1. Run `cd Frontend && npm run build` to check for errors
2. If build fails, fix common issues:
   - Import errors (missing dependencies)
   - TypeScript errors
   - Route structure issues
3. Once build passes, start dev server: `cd Frontend && npm run dev`
4. Verify key pages load: /households, /dependents, /timeline
</action>
  <verify>
  <automated>cd Frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend builds successfully and dev server runs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | Untrusted user input crosses here via API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q01 | S | API calls | accept | Frontend only calls own backend, minimal attack surface |
| T-q02 | I | Environment vars | mitigate | No secrets in frontend, only public API URL |
</threat_model>

<verification>
- `npm run build` passes
- Dev server starts on port 3000
- Pages /households, /dependents, /timeline are accessible
- API calls reach Backend (check network tab)
</verification>

<success_criteria>
Frontend replaced, dependencies fixed, builds without errors, connects to Backend API at localhost:8000, and key pages are accessible.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-x2g-frontend-zip-replacement/260411-x2g-SUMMARY.md`
</output>