'use client';
import { ScannerView } from '@/components/ScannerView';

export default function MedicinePage() {
  return (
    <div className="animate-in fade-in zoom-in-95 duration-500 h-full min-h-[600px]">
      <ScannerView />
    </div>
  );
}
